# Preflight

面向「对 IP 网络环境敏感的工具」用户的网络环境体检产品——在使用这类工具前先检测自己的 IP 环境。一个仓库产出两个实现——在线体检站与本机命令行工具——它们共享同一份判级契约。

## Language

**Preflight CLI**:
本仓库产出的命令行工具，运行在用户本机、能读取本地网络环境，覆盖全部检测项。
_Avoid_: Preflight（单说"Preflight"时永远有歧义，必须带 CLI 或 Web 后缀）

**Preflight Web**:
本仓库产出的网站，运行在 Cloudflare 边缘，只能观察到用户浏览器与网络出口暴露给服务端的信息。
_Avoid_: 官网、门户、Preflight online

**ai-ipcheck**:
Preflight CLI 的前身，曾发布到 PyPI（命令名为 `ipcheck`），已归档、不再维护。提到它只为指代历史行为。
_Avoid_: 旧 CLI、老 CLI（这两个词不体现它是另一份独立实现，而非同一实现的旧版本）

**检测项**:
一条独立的网络环境判定（如"IPv6 是否泄露"），有自己的取值、颜色语义与风险权重。
_Avoid_: 检查、测试项、check

**可在线检测项**:
浏览器 + Cloudflare 边缘即可独立完成的检测项，无需用户本机权限。
_Avoid_: Web 项、线上项

**仅 CLI 项**:
必须读取本机环境（本地 DNS 服务器、系统代理/TUN 状态、`$TZ`）才能完成的检测项，Preflight Web 结构性地做不到。
_Avoid_: 不支持项、本地项

**按需检测项**:
在 Preflight Web 中需用户显式触发才执行的可在线检测项，因其消耗本站共享的第三方配额。Preflight CLI 用用户自己的配额，同一检测项在 CLI 侧自动执行、不构成按需项。
_Avoid_: 付费项、手动项

**信号**:
参与综合结论判定的一条具名判据（如「`$TZ` 时区与出口 IP 时区不一致」）。与检测项是多对一关系：一个检测项可产出多个信号，也可以一个都不产出。
_Avoid_: 指标、因子、风险项

**出口 IP**:
用户流量经代理后离开的公网 IP，即 Cloudflare 边缘观察到的来源地址。Preflight Web 只能看到这一个 IP。
_Avoid_: 公网 IP、真实 IP（"真实 IP"在 CLI 指国内直连回显得到的本机公网 IP，Web 拿不到，一律不用此词）

**分流泄露**:
部分流量绕开代理直接出网的现象。代理只接管了一部分流量，其余仍从本地出口离开。
_Avoid_: 漏代理、代理泄露

**出口 resolver**:
实际替用户向权威 DNS 发起查询的那台服务器。
_Avoid_: 本地 DNS（那是「本机配置的 DNS 服务器」，与出口 resolver 常常不是同一台）

**反射地址**:
STUN 服务器回报的、它所看到的客户端公网地址。
_Avoid_: srflx、公网 IP

**综合结论**:
汇总全部已完成检测项后给出的单一风险定论，分低/中/高三档。
_Avoid_: 总评、评分、安全分

**覆盖度**:
本次检测实际完成的检测项数与总检测项数之比。综合结论永远不得脱离覆盖度单独呈现。
_Avoid_: 完成度、进度

**初步结论**:
IP 风险评分尚未取得时的综合结论形态，取值域只有低与中两档，呈现时必须带显式标注。
_Avoid_: 快速结论、预估结论

**完整结论**:
纳入 IP 风险评分后的综合结论形态，是唯一可能取「高」的形态。
_Avoid_: 最终结论、终评

**数据不足**:
一个信号都没产出时综合结论所处的形态，没有风险档位。它与「低」的区别必须同时体现在文案与配色两处——把没测成说成没问题，是本产品最要防的那类误报。
_Avoid_: 未知风险、无结论、检测中

**需关注项**:
报告结论区按严重度点名、提请用户优先查看的检测项。清单必须区分「产出了贡献综合结论的信号」与「仅作提醒」两类，不得混同——后者再多也不改变档位。
_Avoid_: 问题项、异常项、风险清单
