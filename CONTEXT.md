# ipcheck Web

面向 AI 工具（Claude Code 等）用户的在线网络环境体检站，同时作为已发布 CLI 工具 `ai-ipcheck` 的公开门面。

## Language

**ipcheck CLI**:
已发布到 PyPI 的 Python 命令行工具（包名 `ai-ipcheck`，命令名 `ipcheck`），运行在用户本机、能读取本地网络环境。
_Avoid_: ipcheck（单说"ipcheck"时永远有歧义，必须带 CLI 或 Web 后缀）

**ipcheck Web**:
本仓库构建的网站，运行在 Cloudflare 边缘，只能观察到用户浏览器与网络出口暴露给服务端的信息。
_Avoid_: 官网、门户、ipcheck online

**检测项**:
一条独立的网络环境判定（如"IPv6 是否泄露"），有自己的取值、颜色语义与风险权重。
_Avoid_: 检查、测试项、check

**可在线检测项**:
浏览器 + Cloudflare 边缘即可独立完成的检测项，无需用户本机权限。
_Avoid_: Web 项、线上项

**仅 CLI 项**:
必须读取本机环境（本地 DNS 服务器、系统代理/TUN 状态、`$TZ`、Claude Code 配置）才能完成的检测项，ipcheck Web 结构性地做不到。
_Avoid_: 不支持项、本地项

**按需检测项**:
需用户显式触发才执行的可在线检测项，因其消耗第三方 API 的有限配额。
_Avoid_: 付费项、手动项

**出口 IP**:
用户流量经代理后离开的公网 IP，即 Cloudflare 边缘观察到的来源地址。ipcheck Web 只能看到这一个 IP。
_Avoid_: 公网 IP、真实 IP（"真实 IP"在 CLI 指国内直连回显得到的本机公网 IP，Web 拿不到，一律不用此词）

**综合结论**:
汇总全部已完成检测项后给出的单一风险定论，分低/中/高三档。
_Avoid_: 总评、评分、安全分

**覆盖度**:
本次检测实际完成的检测项数与总检测项数之比。综合结论永远不得脱离覆盖度单独呈现。
_Avoid_: 完成度、进度

**初步结论**:
仅基于自动执行的检测项算出的综合结论，取值域只有低与中两档。
_Avoid_: 快速结论、预估结论

**完整结论**:
纳入按需检测项后重算的综合结论，是唯一可能取「高」的形态。
_Avoid_: 最终结论、终评
